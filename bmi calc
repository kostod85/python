import math
import datetime

m = float(input('Вевдите свой вес '))
l = float(input('Введите свой рост '))
bmi = (m / ( l ** 2 ))*10000
print('Ваш индекс массы тела ')
print( bmi)
if bmi < 20.0:
    print(" ты дрищ толстей")
print(datetime.datetime.now())